package me.sunmc.math.token;

/**
 * A token representing an opening parenthesis {@code (}, {@code [}, or {@code {}}.
 *
 * @author sun-dev
 * @since 1.0.0
 */
public record OpenParenToken() implements Token {
}
