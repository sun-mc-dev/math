package me.sunmc.math.token;

/**
 * A token representing an argument separator {@code ,} in function calls.
 *
 * @author sun-dev
 * @since 1.0.0
 */
public record SeparatorToken() implements Token {
}
