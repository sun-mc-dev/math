package me.sunmc.math.token;

/**
 * A token representing a closing parenthesis {@code )}, {@code ]}, or {@code }}.
 *
 * @author sun-dev
 * @since 1.0.0
 */
public record CloseParenToken() implements Token {
}
